---
type: concept
status: complete
date_created: 2026-04-21
tags: [architecture, backoff, database, jitter, patterns, resilience, retry, system-design]
---


## 3. Exponential Backoff

Thời gian đợi **tăng theo exponential** với mỗi failure.

```
Attempt 1: wait = 100ms
Attempt 2: wait = 200ms
Attempt 3: wait = 400ms
Attempt 4: wait = 800ms
...
```

```go
for attempt := 0; attempt < maxRetries; attempt++ {
    err := callAPI()
    if err == nil {
        return nil
    }

    if attempt < maxRetries-1 {
        delay := time.Duration(math.Pow(2, float64(attempt))) * 100 * time.Millisecond
        time.Sleep(delay)
    }
}
```

### Best for

- Avoid thundering herd
- Reduce stress in the target server
- Most scenarios

---
---

## Retry with Context

```go
func callWithRetry(ctx context.Context, maxRetries int) error {
    for attempt := 0; attempt < maxRetries; attempt++ {
        select {
        case <-ctx.Done():
            return ctx.Err()
        default:
        }

        err := callAPI()
        if err == nil {
            return nil
        }

        if attempt < maxRetries-1 {
            delay := backoffWithJitter(attempt)
            select {
            case <-ctx.Done():
                return ctx.Err()
            case <-time.After(delay):
            }
        }
    }
    return fmt.Errorf("after %d retries", maxRetries)
}
```

---
